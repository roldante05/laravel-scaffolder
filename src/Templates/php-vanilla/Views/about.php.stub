<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><?= $title ?> - {{PROJECT_NAME}}</title>
    {{USE_TAILWIND}}
    <script src="https://cdn.tailwindcss.com"></script>
    {{/USE_TAILWIND}}
</head>
<body class="{{USE_TAILWIND}}bg-slate-100 text-slate-900 antialiased{{/USE_TAILWIND}}">
    
    <div class="{{USE_TAILWIND}}min-h-screen flex{{/USE_TAILWIND}}">
        <?php if (isset($_SESSION['user_id'])): ?>
            <?php require_once __DIR__ . '/layout/sidebar.php'; ?>
        <?php endif; ?>

        <div class="{{USE_TAILWIND}}flex-1 flex flex-col{{/USE_TAILWIND}}">
            <?php if (isset($_SESSION['user_id'])): ?>
                <?php require_once __DIR__ . '/layout/header.php'; ?>
            <?php else: ?>
                <?php require_once __DIR__ . '/nav.php'; ?>
            <?php endif; ?>

            <main class="{{USE_TAILWIND}}p-8{{/USE_TAILWIND}}">
                <div class="bg-white p-10 rounded-3xl shadow-sm border border-slate-100 max-w-4xl mx-auto">
                    <h1 class="text-4xl font-extrabold text-slate-900 mb-6 tracking-tight"><?= $title ?></h1>
                    <div class="prose prose-slate max-w-none text-slate-600 leading-relaxed space-y-4">
                        <p>Somos una plataforma dedicada a simplificar el desarrollo de aplicaciones PHP modernas.</p>
                        <p>Nuestra misión es proporcionar las herramientas necesarias para que los desarrolladores se enfoquen en lo que realmente importa: la lógica de su negocio.</p>
                    </div>
                </div>
            </main>
        </div>
    </div>
</body>
</html>
